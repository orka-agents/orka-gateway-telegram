apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orka-gateway-telegram-data
  labels:
    app.kubernetes.io/name: orka-gateway-telegram
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
